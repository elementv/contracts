To use the application do the following:

1. Create a PostgreSQL database and write its properties in the src/resources/application.properties file.
2. Start the Spring Boot application using Application class.
3. Open http://localhost:8080/downloaddocuments URL in the browser to process the files. The result of the operation will be shown on the page.